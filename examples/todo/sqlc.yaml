version: "2"
sql:
  - engine: postgresql
    queries: queries.sql
    schema: schema.sql
    gen:
      go:
        package: db
        out: db
        sql_package: pgx/v5
        emit_prepared_queries: false
        overrides:
          - db_type: timestamptz
            nullable: false
            go_type:
              type: "time.Time"
          - db_type: timestamptz
            nullable: true
            go_type:
              type: "time.Time"
              pointer: true
